% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_segments.R
\name{elcf4r_read_refit}
\alias{elcf4r_read_refit}
\title{Read and normalize the REFIT cleaned household dataset}
\usage{
elcf4r_read_refit(
  path = "data-raw",
  house_ids = NULL,
  channels = "Aggregate",
  start = NULL,
  end = NULL,
  tz = "UTC",
  resolution_minutes = 1L,
  agg_fun = c("mean", "sum", "last"),
  n_max = NULL,
  drop_na_load = TRUE
)
}
\arguments{
\item{path}{Path to a REFIT file or to a directory containing
\code{CLEAN_House*.csv} files.}

\item{house_ids}{Optional vector of house identifiers to keep. These are
matched against file stems such as \code{"CLEAN_House1"}.}

\item{channels}{Character vector of load channels to extract. Defaults to
\code{"Aggregate"}.}

\item{start}{Optional inclusive lower time bound.}

\item{end}{Optional inclusive upper time bound.}

\item{tz}{Time zone used to parse timestamps.}

\item{resolution_minutes}{Target regular resolution in minutes for the
normalized output. Defaults to \code{1}.}

\item{agg_fun}{Aggregation used when resampling to the target grid. One of
\code{"mean"}, \code{"sum"} or \code{"last"}.}

\item{n_max}{Optional maximum number of raw rows to read per file.}

\item{drop_na_load}{Logical; if \code{TRUE}, rows with missing load values are
dropped after resampling.}
}
\value{
A normalized data frame with REFIT household data.
}
\description{
Read one or more \code{CLEAN_House*.csv} files from the REFIT dataset, optionally
select appliance channels, resample them to a regular time grid, and return a
normalized long-format panel.
}
