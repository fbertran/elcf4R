% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elcf4r_fit_kwf_clustered.R
\name{elcf4r_fit_kwf_clustered}
\alias{elcf4r_fit_kwf_clustered}
\title{Fit a clustered KWF model for daily load curves}
\usage{
elcf4r_fit_kwf_clustered(
  segments,
  covariates = NULL,
  target_covariates = NULL,
  wavelet = "la12",
  bandwidth = NULL,
  use_mean_correction = TRUE,
  max_clusters = 10L,
  nstart = 30L,
  cluster_seed = NULL,
  weights = NULL,
  recency_decay = NULL,
  clustering = NULL
)
}
\arguments{
\item{segments}{Matrix or data frame of past daily load curves in
chronological order.}

\item{covariates}{Optional data frame with one row per segment.}

\item{target_covariates}{Optional one-row data frame for the target day.}

\item{wavelet}{Wavelet filter name passed to \code{\link[wavelets:dwt]{wavelets::dwt()}}. Defaults to
\code{"la12"}.}

\item{bandwidth}{Optional positive bandwidth for the Gaussian kernel in the
underlying KWF fit.}

\item{use_mean_correction}{Logical; if \code{TRUE}, apply the approximation/detail
correction in the underlying KWF fit.}

\item{max_clusters}{Maximum number of candidate clusters considered by the
Sugar jump heuristic.}

\item{nstart}{Number of random starts for \code{kmeans}.}

\item{cluster_seed}{Deprecated and ignored. Clustered KWF now uses
deterministic non-random starts.}

\item{weights}{Optional prior weights passed through to the base KWF fit.}

\item{recency_decay}{Optional recency prior passed through to the base KWF
fit.}

\item{clustering}{Optional \code{elcf4r_kwf_clusters} object. When supplied, the
stored clustering model is reused instead of being refit.}
}
\value{
An object of class \code{elcf4r_model} with \code{method = "kwf_clustered"}.
}
\description{
Cluster dyadically resampled daily curves in a wavelet-energy feature space
and use the resulting cluster labels as the grouping structure inside the
KWF forecast.
}
