% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elcf4r_benchmark.R
\name{elcf4r_benchmark}
\alias{elcf4r_benchmark}
\title{Run a rolling-origin benchmark on a normalized panel}
\usage{
elcf4r_benchmark(
  panel,
  benchmark_index = NULL,
  methods = NULL,
  entity_ids = NULL,
  cohort_size = NULL,
  train_days = 28L,
  test_days = 5L,
  benchmark_name = NULL,
  dataset = NULL,
  use_temperature = TRUE,
  method_args = NULL,
  include_predictions = TRUE,
  thermosensitivity_panel = NULL,
  benchmark_index_carry_cols = NULL,
  seed = NULL,
  tz = "UTC"
)
}
\arguments{
\item{panel}{Normalized panel data, typically returned by one of the
\verb{elcf4r_read_*()} adapters.}

\item{benchmark_index}{Optional day-level index. If \code{NULL}, it is created
with \code{elcf4r_build_benchmark_index()}.}

\item{methods}{Character vector of method names to evaluate. Supported values
are \code{"gam"}, \code{"mars"}, \code{"kwf"}, \code{"kwf_clustered"} and \code{"lstm"}. If
\code{NULL}, the runner uses \code{gam}, \code{mars}, \code{kwf}, \code{kwf_clustered} and adds
\code{lstm} only when its backend is available.}

\item{entity_ids}{Optional character vector of entity IDs to benchmark.}

\item{cohort_size}{Optional maximum number of eligible entities to keep after
sorting by \code{entity_id}.}

\item{train_days}{Number of days in each training window.}

\item{test_days}{Number of one-day rolling test origins per entity.}

\item{benchmark_name}{Optional benchmark identifier. If \code{NULL}, one is
derived from the dataset label and benchmark design.}

\item{dataset}{Optional dataset label overriding \code{unique(panel$dataset)}.}

\item{use_temperature}{Logical; if \code{TRUE}, methods that support temperature
will use it when non-missing temperature information is available for the
current window.}

\item{method_args}{Optional named list of per-method argument overrides.}

\item{include_predictions}{Logical; if \code{TRUE}, return a long table of saved
point forecasts and naive forecasts.}

\item{thermosensitivity_panel}{Optional normalized panel used for
thermosensitivity classification. Defaults to \code{panel}.}

\item{benchmark_index_carry_cols}{Optional \code{carry_cols} passed to
\code{elcf4r_build_benchmark_index()} when \code{benchmark_index} is not supplied.}

\item{seed}{Optional integer seed forwarded to methods that support
user-supplied seeding, such as LSTM, unless overridden in \code{method_args}.}

\item{tz}{Time zone used to derive dates and within-day positions.}
}
\value{
An object of class \code{elcf4r_benchmark} with elements \code{results},
\code{predictions}, \code{cohort_index}, \code{spec} and \code{backend}.
}
\description{
Evaluate the package forecasting methods on a normalized panel using a
deterministic rolling-origin design. The runner supports the current
temperature-aware \code{gam}, \code{mars}, \code{kwf}, \code{kwf_clustered} and \code{lstm} wrappers
and returns both aggregate scores and, optionally, saved point forecasts.
}
\examples{
id1 <- subset(
  elcf4r_iflex_example,
  entity_id == unique(elcf4r_iflex_example$entity_id)[1]
)
keep_dates <- sort(unique(id1$date))[1:6]
panel_small <- subset(id1, date \%in\% keep_dates)
\donttest{
bench <- elcf4r_benchmark(
  panel = panel_small,
  methods = "gam",
  cohort_size = 1,
  train_days = 4,
  test_days = 1,
  include_predictions = TRUE
)
head(bench$results)
}
}
