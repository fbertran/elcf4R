% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_segments.R
\name{elcf4r_build_daily_segments}
\alias{elcf4r_build_daily_segments}
\title{Build daily load-curve segments from a normalized panel}
\usage{
elcf4r_build_daily_segments(
  data,
  id_col = "entity_id",
  timestamp_col = "timestamp",
  value_col = "y",
  temp_col = "temp",
  carry_cols = NULL,
  expected_points_per_day = NULL,
  resolution_minutes = NULL,
  complete_days_only = TRUE,
  drop_na_value = TRUE,
  tz = "UTC"
)
}
\arguments{
\item{data}{Data frame containing at least entity id, timestamp and load.}

\item{id_col}{Name of the entity identifier column.}

\item{timestamp_col}{Name of the timestamp column.}

\item{value_col}{Name of the load column.}

\item{temp_col}{Optional name of a temperature column used to derive day
summaries.}

\item{carry_cols}{Optional day-level columns to propagate into the returned
covariate table. Their first non-missing value within each day is kept.}

\item{expected_points_per_day}{Expected number of samples per day. If \code{NULL},
it is derived from \code{resolution_minutes}.}

\item{resolution_minutes}{Sampling resolution in minutes. If \code{NULL}, it is
inferred from timestamps or from a \code{resolution_minutes} column.}

\item{complete_days_only}{If \code{TRUE}, incomplete or duplicated days are
dropped from the output.}

\item{drop_na_value}{If \code{TRUE}, days with missing load values are dropped.}

\item{tz}{Time zone used to derive dates and within-day positions.}
}
\value{
A list with components \code{segments}, \code{covariates}, \code{resolution_minutes}
and \code{points_per_day}.
}
\description{
Convert a long-format load table into one row per entity-day and one column
per within-day time index. This is the matrix representation required by
functional load-curve models and rolling benchmark scripts.
}
\examples{
id1 <- subset(
  elcf4r_iflex_example,
  entity_id == unique(elcf4r_iflex_example$entity_id)[1]
)
daily <- elcf4r_build_daily_segments(id1, carry_cols = "participation_phase")
dim(daily$segments)
names(daily$covariates)
}
