% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elcf4r_fit_lstm.R
\name{elcf4r_fit_lstm}
\alias{elcf4r_fit_lstm}
\title{Fit an LSTM model for daily load curves}
\usage{
elcf4r_fit_lstm(
  segments,
  covariates = NULL,
  use_temperature = FALSE,
  lookback_days = 1L,
  units = 16L,
  epochs = 10L,
  batch_size = 8L,
  validation_split = 0,
  seed = 123L,
  verbose = 0L
)
}
\arguments{
\item{segments}{Matrix or data frame of past daily load curves
(rows are days, columns are time points).}

\item{covariates}{Optional data frame with one row per training day.}

\item{use_temperature}{Logical. If \code{TRUE}, use \code{temp_mean} from
\code{covariates} as an additional input feature when available.}

\item{lookback_days}{Number of past daily curves used as one training input.}

\item{units}{Number of hidden units in the LSTM layer.}

\item{epochs}{Number of training epochs.}

\item{batch_size}{Batch size used in \code{keras3::fit()}.}

\item{validation_split}{Validation split passed to \code{keras3::fit()}.}

\item{seed}{Random seed passed to TensorFlow.}

\item{verbose}{Verbosity level passed to \code{keras3::fit()} and \code{predict()}.}
}
\value{
An object of class \code{elcf4r_model} with \code{method = "lstm"}.
}
\description{
The LSTM implementation uses one or more previous daily curves to predict the
next daily curve. When \code{use_temperature = TRUE} and \code{temp_mean} is available
in \code{covariates}, the daily mean temperature is added as a second input
feature repeated across the within-day time steps.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
if (requireNamespace("keras3", quietly = TRUE) &&
    requireNamespace("tensorflow", quietly = TRUE) &&
    suppressWarnings(
      tryCatch(
        utils::getFromNamespace("is_keras_available", "keras3")(),
        error = function(e) FALSE
      )
    )) {
  id1 <- subset(
    elcf4r_iflex_example,
    entity_id == unique(elcf4r_iflex_example$entity_id)[1]
  )
  daily <- elcf4r_build_daily_segments(id1)
  fit <- elcf4r_fit_lstm(
    segments = daily$segments[1:10, ],
    covariates = daily$covariates[1:10, ],
    use_temperature = TRUE,
    epochs = 1,
    units = 4,
    batch_size = 2,
    verbose = 0
  )
  length(predict(fit))
}
\dontshow{\}) # examplesIf}
}
