% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elcf4r_fit_mars.R
\name{elcf4r_fit_mars}
\alias{elcf4r_fit_mars}
\title{Fit a MARS model for load curves}
\usage{
elcf4r_fit_mars(data, use_temperature = FALSE)
}
\arguments{
\item{data}{Data frame with columns \code{y} (load), \code{time_index}
(numeric or factor for within day position), \code{dow}, \code{month},
optional \code{temp} and other covariates.}

\item{use_temperature}{Logical. If \code{TRUE}, include temperature
as smooth effect and interactions.}
}
\value{
An \code{elcf4r_model} object with \code{method = "mars"}.
}
\description{
Fit a MARS model for load curves
}
\examples{
id1 <- subset(
  elcf4r_iflex_example,
  entity_id == unique(elcf4r_iflex_example$entity_id)[1]
)
train_data <- subset(id1, date < sort(unique(id1$date))[11])
test_data <- subset(id1, date == sort(unique(id1$date))[11])
fit <- elcf4r_fit_mars(train_data[, c("y", "time_index", "dow", "month", "temp")], TRUE)
pred <- predict(fit, newdata = test_data[, c("y", "time_index", "dow", "month", "temp")])
length(pred)
}
